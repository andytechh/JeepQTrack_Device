<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Internet / Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- GPS / Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Required if geofencing/location must continue in background -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Phone state -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Keep device awake while passenger counting is active -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        SMS permission is intentionally NOT requested.
        SMS notifications are sent through UniSMS/API from SmsService,
        not directly through the device SIM.
    -->
    <!--
    <uses-permission android:name="android.permission.SEND_SMS" />
    -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YOLOv8TfLite"
        tools:targetApi="31">

        <!--
            Setup screen is the launcher.
            It configures the jeepney/device before opening MainActivity.
        -->
        <activity
            android:name=".activities.SetupActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Main passenger-counting / GPS / geofence screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Geofence transition receiver -->
        <receiver
            android:name=".services.GeofenceBroadcastReceiver"
            android:exported="false" />

        <!-- Optional GPU/OpenCL libraries -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <uses-native-library
            android:name="libOpenCL-pixel.so"
            android:required="false" />

    </application>

</manifest>